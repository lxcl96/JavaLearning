

Object字节处理流：对象字节处理流
    1、读写顺序要一致【必须】
    2、要求存储/读取的数据必须实现了Serializable接口，是可序列化的
    3、序列化对象时，默认将里面的所有属性都进行序列化，除了static或transient修饰的成员
    4、序列化的类中建议添加SerialVersionUID，为了提高版本的兼容性
    5、序列化对象时，要求里面的属性的类型也需要实现序列化接口 ？？
    6、序列化具有可继承性，也就是如果某类已经实现了序列化，则他的所有子类也默认实现了序列化如Integer