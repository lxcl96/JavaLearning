【类加载】
反射机制是java实现动态语言的关键，也是通过反射实现类的动态加载
类加载分为： Display01.java
    1、静态加载：编译时就去看这个类存不存在，不管是不是一定会用到，依赖性太强【饿汉式】
    2、动态加载：在运行时如果用到了就加载这个类【再报错】，否则就不加载这个类【不会报错】，降低了依赖性


【类的加载时机】
1、当对象被创建时（new） //静态加载
2、当子类被加载时，父类也被加载  //静态加载
3、调用类中的静态成员时  //静态加载
4、通过反射  //动态加载